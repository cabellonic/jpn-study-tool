<!-- MainWindow.xaml -->
<Window
    x:Class="JpnStudyTool.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JpnStudyTool"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:JpnStudyTool.ViewModels"
    xmlns:models="using:JpnStudyTool.Models"
    mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <!-- Title Row -->
            <RowDefinition Height="Auto"/>
            <!-- ListView Row -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Sentence History" FontSize="20" Margin="0,0,0,10" Grid.Row="0"/>

        <!-- Bind to ViewModel's collection -->
        <ListView Grid.Row="1"
                  ItemsSource="{Binding SentenceHistory}"
            SelectionMode="Single"
                  IsItemClickEnabled="False">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:SentenceHistoryItem">
                    <TextBlock Text="{x:Bind Sentence}"
                               TextTrimming="CharacterEllipsis"
                               Margin="5"/>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

    </Grid>
</Window>